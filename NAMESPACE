exportClasses(AnnotationHubMetadata, AnnotationHubRecipe)
export(AnnotationHubMetadata, AnnotationHubRecipe)
export(constructAnnotationHubMetadataFromOriginalFilePath,
 postProcessMetadata,
 constructAnnotationHubMetadataFromJsonPath)
import(RJSONIO)
importFrom(BiocInstaller, biocVersion)

export(recipe1,
       constructSeqInfo)

exportMethods(
    show,
    run,
    runWild,
    recipeName,
    inputFiles,
    outputFile,
    annotationHubRoot
    )
