exportClasses(AnnotationHubMetadata, AnnotationHubRecipe,
    DataProvider, EncodeDCCProvider, EnsemblGtfProvider,
    UCSCTableHubProvider)

export(AnnotationHubMetadata, AnnotationHubRecipe,
    EncodeDCCProvider, EnsemblGtfProvider, UCSCTableHubProvider)

export(constructAnnotationHubMetadataFromSourceFilePath,
  postProcessMetadata,
  constructMetadataFromJsonPath)

import(RJSONIO)

importFrom(BiocInstaller, biocVersion)

importFrom(IRanges, metadata, "metadata<-")

importFrom(Rsamtools, indexFa)

export(
       to.json,
       to.list,
       constructSeqInfo,
       extendedBedToGRanges,
       extendedBedWithAuxiliaryTableToGRanges,
       ensemblGtfToGRanges,
       rtrackLayerImport,
       EncodeImporter)

exportMethods(
    show,
    run,
    recipeName,
    inputFiles,
    outputFile,
    metadata,
    "metadata<-",
    metadataTable,
    assembleParams,
    createResource
    )

