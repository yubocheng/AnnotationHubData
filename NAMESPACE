exportClasses(AnnotationHubMetadata, AnnotationHubRecipe,
    ImportPreparer, EnsemblGtfImportPreparer, EncodeImportPreparer)

export(AnnotationHubMetadata, AnnotationHubRecipe,
    EnsemblGtfImportPreparer)

export(newResources,
  constructAnnotationHubMetadataFromSourceFilePath,
  postProcessMetadata,
  constructMetadataFromJsonPath)

import(rjson)

importFrom(RCurl, getURL)

importFrom(BiocInstaller, biocVersion)

importFrom(IRanges, metadata, "metadata<-")

importFrom(Rsamtools, indexFa)

importFrom(rtracklayer, import)

export(jsonPath,
       fromJson,
       toJson,
       constructSeqInfo,
       extendedBedToGRanges,
       extendedBedWithAuxiliaryTableToGRanges,
       ensemblGtfToGRanges,
       rtrackLayerImport,
       EncodeImportPreparer,
       EncodeBaseURL)

exportMethods(
    show,
    run,
    recipeName,
    inputFiles,
    outputFile,
    metadata,
    "metadata<-",
    metadataTable,
    assembleParams,
    createResource
    )

